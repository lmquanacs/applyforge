{
  "name": "applyforge",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "build:frontend": "pnpm --filter frontend build",
    "deploy:infra": "pnpm --filter infra deploy"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
